@{
    ViewBag.Title = "Top Scores";
}
<div class="page-header">
    <h2>@ViewBag.Title</h2>
</div>

<div class="well">
    <ul>
    @foreach (var answer in Model)
    {
        <li>@answer.User (@answer.Score)</li>
    }
    </ul>
</div>